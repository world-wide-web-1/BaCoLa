if [ "$1" = "@update" ] ; then
  #sh -c "$(curl -fsSL 'https://raw.githubusercontent.com/Christian-Rice/BaCoLa/main/update.sh')" &&
  echo "@update is currently unavaliable." &&
  exit 1;
else
  node index.js $1;
  exit 0;
fi